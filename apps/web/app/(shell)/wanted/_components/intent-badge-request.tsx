// IntentBadgeRequest — the request-colored intent pill.
//
// Verbatim port of `.intent-pill.intent-pill-request` from the mockup.
// CSS is already defined in apps/web/app/styles/wanted.css.

export function IntentBadgeRequest() {
  return <span className="intent-pill intent-pill-request">REQUEST</span>;
}
