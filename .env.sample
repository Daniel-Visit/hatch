# === Agentic Layer ===
ANTHROPIC_API_KEY=

# === Optional: multi-provider LLM routing ===
# OPENAI_API_KEY=
# OLLAMA_BASE_URL=http://localhost:11434

# === Observability dashboard (disabled — enable in adaptation_manifest.json) ===
# OBSERVABILITY_SERVER_URL=http://localhost:9403
# ORCHESTRATOR_BACKEND_URL=http://localhost:9403
# OBSERVABILITY_PROJECT_ID=hatch

# ─── apps/web (Vercel project vars in prod, .env.local in dev) ───────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=https://vcbdtjjkkwryvmqbflah.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_d65F3PSi03kcRFIlnkNimg_JJ4mxxk2

# Server-only — never expose to client
SUPABASE_SERVICE_ROLE_KEY=

# Vercel Cron auth
CRON_SECRET=

# MCP server URL (used by the "Copy MCP config" UI in Phase 9)
NEXT_PUBLIC_MCP_URL=http://localhost:8080

# ─── apps/mcp (Railway env vars in prod) ─────────────────────────────────
PORT=8080
SUPABASE_URL=https://vcbdtjjkkwryvmqbflah.supabase.co
LOG_LEVEL=info

# ─── apps/web — Web Push (Phase 6, opt-in) ───
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
