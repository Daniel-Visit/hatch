// Re-export the auto-generated Supabase Database type so it can be imported
// from any workspace via `@hatch/shared` instead of reaching across apps.
export type { Database } from '../../../apps/web/lib/supabase/types';
